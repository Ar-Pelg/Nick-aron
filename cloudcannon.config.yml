paths:
  uploads: public/uploads
  static: ""

# 1. DEZE REGEL IS CRUCIAAL
# Hiermee dwingen we af: "Als ik home.json bewerk, update dan de homepage (url: /)"
file_config:
  - glob: "content/home.json"
    url: /
    _enabled_editors:
      - visual
      - data

# 2. Algemene instellingen
collections_config:
  pages:
    path: content
    glob: "**/*.json"
    name: "Pagina's"
    output: false
    url: /
    _enabled_editors:
      - visual
      - data

# 3. Ruim de editor op
source_editor:
  ignore:
    - "node_modules"
    - ".git"
    - "src"
    - "*.tsx"
    - "*.ts"
    - "package.json"
    - "tsconfig.json"
    - "vite.config.ts"

# 4. Input labels
_inputs:
  hero_label:
    label: "Label"
  hero_title_start:
    label: "Titel (Start)"
  hero_title_italic:
    label: "Titel (Cursief)"
  hero_description:
    label: "Omschrijving"
    type: textarea
